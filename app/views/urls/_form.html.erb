<%= form_for(@url) do |f| %>
  <% if @url.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@url.errors.count, "error") %> prohibited this url from being saved:</h2>

         <ul>
         <% @url.errors.full_messages.each do |msg| %>
           <li><%= msg %></li>
         <% end %>
         </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :long_url %><br />
    <%= f.text_field :long_url %>
  </div>
  <!-- don't need this because we generate this for the user **see models/url.rb -->
  <!-- <div class="field">
    <%= f.label :short_url_slug %><br />
    <%= f.text_field :short_url_slug %>
  </div> -->
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
